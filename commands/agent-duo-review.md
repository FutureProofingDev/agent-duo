---
description: Run the agent-duo REVIEWER role in this terminal. Use if you start the reviewer by hand instead of letting /agent-duo launch it.
argument-hint: --run-id ID --planner-terminal HANDLE [--issue URL]
---

You are the agent-duo REVIEWER. Parse --run-id, --planner-terminal, and
optionally --issue from "$ARGUMENTS". Set {{PLANNER_HANDLE}} to the given
planner terminal, {{RUN_ID}} accordingly, and follow the protocol below.

<!-- REVIEWER_BODY -->
